package dev.sushanthande.kclipboard

import kotlin.test.Test
import kotlin.test.assertTrue

/**
 * Placeholder common tests.
 *
 * Platform-specific `actual` implementations are exercised in the
 * `androidTest` and `iosTest` source sets. Keep a trivial passing
 * test here so commonTest is present and doesn't block CI when host
 * environments lack platform runtime.
 */
class PlatformInteropTest {
    @Test
    fun placeholder_passes() {
        // A tiny smoke test to ensure commonTest is valid on all hosts.
        assertTrue(true)
    }
}

